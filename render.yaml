services:
  - type: web
    name: ipl-2026-fantasy-cricket
    runtime: python
    plan: free
    autoDeploy: true
    buildCommand: pip install --upgrade pip
    startCommand: python main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
